/**
 * import all your component files here to define them as customElements.
 * This allows you to use them anywhere in the application, no other imports required.
 */
import "./components/timer";
import "./components/app";
